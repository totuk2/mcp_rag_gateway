# Upstream MCP servers. server_id must match [A-Za-z0-9._-]+ and must not contain "__".
servers:
  echo:
    transport: stdio
    command: python
    args: ["servers/echo_server.py"]
    # cwd: null
    # env: {}

  # example_remote:
  #   transport: sse
  #   url: "http://127.0.0.1:9000/sse"
  #   headers: {}

  # example_streamable:
  #   transport: streamable_http
  #   url: "http://127.0.0.1:9000/mcp"
  #   headers: {}
